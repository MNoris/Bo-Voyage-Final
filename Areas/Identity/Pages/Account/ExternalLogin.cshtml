@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "S'inscrire";
}

<h1>@ViewData["Title"]</h1>
<h4>Associez votre compte @Model.LoginProvider.</h4>
<hr />

<p class="text-info">
    Vous vous êtes connecté avec succès en tant que <strong>@Model.LoginProvider</strong>.
    Veuillez entrer une adresse e-mail pour ce site plus bas et cliquer sur le bouton s'inscrire pour finaliser le processus de connection.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">S'inscrire</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
